<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% block title %}ES Plateforme{% endblock %}</title>

  {% block stylesheets %}
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  {% endblock %}

</head>

<body>
  <div class="container">
    <div id="header" class="jumbotron">
      <h1>Esteli Solidarité</h1>
      <p>
        Esteli Solidarité vous permet de réaliser des projets humanitaires aux Nicaragua, encadrés et formateurs.
      </p>
      <p>
        <a class="btn btn-primary btn-lg" href="https://estelisolidarite.wordpress.com/">
            À Propos »
        </a>
      </p>
    </div>

    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
      Connecté en tant que <a href="{{ path('fos_user_profile_show') }}">{{ app.user.username }}</a>
      -
      <a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
    {% else %}
      <a href="{{ path('fos_user_security_login') }}">Connexion</a>
      -
      <a href="{{ path('fos_user_registration_register') }}">Créer un compte</a>
    {% endif %}

    <div class="row">
      <div id="menu" class="col-md-3">

        {% if is_granted("ROLE_MEMBRE") %}
          <h3>Espace membre</h3>
          <ul class="nav nav-pills nav-stacked">
            <li><a href="{{ path('fos_message_inbox') }}">Messagerie</a></li>
          </ul>
        {% endif %}
      
        <h3>Les annonces</h3>
        <ul class="nav nav-pills nav-stacked">
          <li><a href="{{ path('es_platform_home') }}">Accueil</a></li>
          {% if is_granted('ROLE_AUTEUR') %}
            <li><a href="{{ path('es_platform_add') }}">Ajouter une annonce</a></li>
            <li><a href="{{ path('es_platform_self_adverts') }}">Mes annonces</a></li>
          {% endif %}
        </ul>

        <h4>Dernières annonces</h4>
        {{ render(controller("ESPlatformBundle:Advert:menu", {'limit': 3})) }}
      </div>
      <div id="content" class="col-md-9">
        {% block body %}
        {% endblock %}
      </div>
    </div>

    <hr>

    <footer>
      <p>The sky's the limit © {{ 'now'|date('Y') }} and beyond.</p>
    </footer>
  </div>

  {% block javascripts %}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  {% endblock %}

</body>
</html>