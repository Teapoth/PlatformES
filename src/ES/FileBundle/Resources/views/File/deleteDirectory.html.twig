{% extends "ESFileBundle::layout.html.twig" %}

{% block title %}
	{{ 'Supprimer un dossier'|trans }} - {{ parent() }}
{% endblock %}
 
{% block esfile_body %}

  <h2>{{ 'Supprimer un dossier'|trans }}</h2>

  <p>
	  {{ 'Êtes vous certain de vouloir supprimer le dossier'|trans }} "{{ directory.name }}" {{ 'et tout son contenu ?'|trans }}
  </p>

  <form action="{{ path('es_file_delete_directory', {'id': directory.id}) }}" method="post">
    <input type="submit" value="{{ 'Supprimer'|trans }}" class="btn btn-danger" />
    {{ form_rest(form) }}
  </form>

{% endblock %}